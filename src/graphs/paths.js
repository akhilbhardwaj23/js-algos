class Paths{
    /**
     * 
     * @param {Graph} G 
     * @param {Source} s 
     */
    constructor(G, s){

    }

    /**
     * @param {destination vertex} v
     * returns true if there is a path from source s to destination v
     */
    hasPathTo = (v) =>{

    }

    /**
     * @param {destination vertext} v
     * Path from source s to destination v; returns null if no such path
     */
    pathTo = (v) =>{
        
    }
}